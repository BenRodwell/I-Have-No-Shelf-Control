<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>I Have No Shelf Control - Book Review Database Message Board</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <nav>

    {{#if loggedIn}}

    <button id="logout" class="btn-no-style">Logout</button>

    {{else}}
  <header class="header">
    <h1>I Have No Shelf Control</h1>
  </header>
  <header class="nav a">
    <a href="/">Home</a>
    <a href="/login">Sign Up/Login In</a>
    <a href="/login">Account</a>
    <input type="submitSearch" value="Search">
    <button onclick="search">Submit</button>
  </header>
    {{/if}}
  </nav>
  <main>
    <div class="gallery">
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}

  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>